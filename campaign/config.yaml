app:
  name: campaign-service
  env: development
  port: "8083"
  grpc_port: "50054"
  debug: true

postgres:
  host: localhost
  port: "5432"
  user: postgres
  password: postgres
  database: campaign_db
  ssl_mode: disable
  max_idle_conns: 10
  max_open_conns: 100

redis:
  host: localhost
  port: "6379"
  password: ""
  db: 1
  pool_size: 10

auth:
  grpc_addr: localhost:50051
  timeout: 3s
